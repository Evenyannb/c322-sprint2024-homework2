spring:
  datasource:
    url: jdbc:postgresql://localhost:6001/guiter
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: always
